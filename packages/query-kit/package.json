{
  "name": "@sv-kit/query-kit",
  "version": "1.0.0",
  "description": "Headless query/cache core: dedupe, SWR, retry/backoff, TTL/GC, cancel, prefetch, invalidate, and hydrate/dehydrate.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "sideEffects": false,
  "license": "MIT",
  "keywords": [
    "query",
    "cache",
    "swr",
    "retry",
    "abort",
    "hydrate"
  ],
  "scripts": {
    "clean": "node -e \"try{require('fs').rmSync('dist',{recursive:true,force:true})}catch{}\"",
    "build": "tsc -p tsconfig.json",
    "test": "echo no-tests"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  }
}
