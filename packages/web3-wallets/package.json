{
  "name": "@svelte-kits/web3-wallets",
  "version": "0.1.0",
  "description": "Headless Web3 wallets core for EIP-1193: connectors, auto-connect, chain switch/add, signing, tx, and events.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": ["dist", "README.md"],
  "sideEffects": false,
  "license": "MIT",
  "keywords": ["web3", "wallet", "eip-1193", "metamask", "walletconnect", "coinbase"],
  "scripts": {
    "clean": "node -e \"try{require('fs').rmSync('dist',{recursive:true,force:true})}catch{}\"",
    "build": "tsc -p tsconfig.json",
    "test": "echo no-tests"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  },
  "engines": { "node": ">=18" },
  "publishConfig": { "access": "public" }
}
